{{ include('common_design_subtheme:ocha-key-figures-list', {
    figures,
    view_all: view_all ? true : false,
    view_all_info: view_all_info ? view_all_info : {},
    jsonld: jsonld ? jsonld : ''
  }, only)
}}


hr_paragraphs.webhook:
  path: '/webhook/listen'
  defaults:
    _controller: 'hr_paragraphs.webhook_controller:listen'
    _title: 'Listen to incoming webhooks'
  methods: [POST]
  requirements:
    _access: 'TRUE'
